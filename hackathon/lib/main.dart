import 'package:flutter/material.dart';

import 'study_together_base.dart';
void main() => runApp(StudyTogetherBase());

class StudyTogetherMain extends StatelessWidget {
  const StudyTogetherMain({super.key});

  @override
  Widget build(BuildContext context) {
    return StudyTogetherBase();
  }
}
